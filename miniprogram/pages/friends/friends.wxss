page {
  height: 100vh;
  display: flex;
  flex-direction: column;
}
.scrollarea {
  flex: 1;
  overflow-y: hidden;
}
.friend-item {
  margin-top: 20rpx;
  text-align: center;
}
.friend-item:last-child {
  padding-bottom: env(safe-area-inset-bottom);
}
